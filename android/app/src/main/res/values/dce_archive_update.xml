<?xml version="1.0" encoding="utf-8"?>
<resources>
    <item type="id" name="dce_archive_update_notice" />
    <!-- A short-lived translucent activity lets the update card dim/blur the host safely. -->
    <style name="DceArchiveUpdateTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:colorAccent">@android:color/transparent</item>
    </style>
</resources>
